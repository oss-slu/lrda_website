# =============================================================================
# Where's Religion? - Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# Run: cp .env.example .env.local
#
# NOTE: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# Never put secrets in NEXT_PUBLIC_ variables.
# =============================================================================

# -----------------------------------------------------------------------------
# API Server Configuration
# -----------------------------------------------------------------------------
# URL of the backend API server (Hono + better-auth)
# For local development, this is typically http://localhost:3002
#
NEXT_PUBLIC_API_URL=http://localhost:3002

# -----------------------------------------------------------------------------
# RERUM API (Backend Data Store)
# -----------------------------------------------------------------------------
# For local development, use the Docker setup (see server/README.md)
# The local server runs at localhost:3001
#
NEXT_PUBLIC_RERUM_PREFIX=http://localhost:3001/

# -----------------------------------------------------------------------------
# OpenAI API (Optional - for AI features)
# -----------------------------------------------------------------------------
# Only needed if working on AI-powered features
# Get your API key from https://platform.openai.com/api-keys
#
NEXT_PUBLIC_OPENAI_API_KEY=
NEXT_PUBLIC_OPENAI_API_URL=https://api.openai.com/v1/chat/completions

# -----------------------------------------------------------------------------
# Google Maps API (Optional - for map features)
# -----------------------------------------------------------------------------
# Only needed if working on map-related features
# Get your API key from https://console.cloud.google.com/google/maps-apis
#
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
# -----------------------------------------------------------------------------
# S3 Media Storage (Local Development with LocalStack)
# -----------------------------------------------------------------------------
# For local development, use LocalStack (included in docker-compose.yml)
# Run: pnpm docker:up (starts LocalStack along with MongoDB)
#
# Set to 'true' to use local S3 (LocalStack) instead of production S3
NEXT_PUBLIC_USE_LOCAL_S3=true

# For local development with LocalStack:
NEXT_PUBLIC_S3_PROXY_PREFIX=http://localhost:3000/api/s3-local/
LOCALSTACK_URL=http://localhost:4566
S3_BUCKET_NAME=livedreligion-local

# For production (leave empty for local dev):
# NEXT_PUBLIC_S3_PROXY_PREFIX=https://your-s3-proxy-server.com/
