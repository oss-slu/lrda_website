rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all files for development
    // IMPORTANT: Replace with proper rules before production deployment
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
